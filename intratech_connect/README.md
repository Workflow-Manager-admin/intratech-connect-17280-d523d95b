# Lightweight React Template for KAVIA

**Directory Name Update:**  
This folder has been renamed. Previously it was `intratech_connect/`, now it is `frontend/`.

This project provides a minimal React template with a clean, modern UI and minimal dependencies.

---

_All references to project commands remain the same (use `npm start`, `npm test`, `npm run build`), but you now should operate in the_ `frontend/` _directory for all frontend development and in_ `backend/` _for server API development._

## Features

- **Lightweight**: No heavy UI frameworks - uses only vanilla CSS and React
- **Modern UI**: Clean, responsive design with KAVIA brand styling
- **Fast**: Minimal dependencies for quick loading times
- **Simple**: Easy to understand and modify

## Getting Started

Change into the `frontend/` directory:

```
cd frontend
```

You can then run:

### `npm start`

Runs the app in development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

### `npm test`

Launches the test runner in interactive watch mode.

### `npm run build`

Builds the app for production to the `build` folder.\
It correctly bundles React in production mode and optimizes the build for the best performance.

_Refer to main project README for directory details._

