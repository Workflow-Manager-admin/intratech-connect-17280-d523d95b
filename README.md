# intratech-connect-17280-d523d95b

## Project Structure Change

As of the latest update, the main directories have been renamed for clarity and convention:

- `frontend/` - contains the ReactJS codebase (previously `intratech_connect`)
- `backend/`  - contains the Node backend API and data (previously `intratech_connect_backend`)

Be sure to update your commands and paths accordingly!